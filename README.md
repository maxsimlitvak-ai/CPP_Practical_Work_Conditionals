Тема: Умовний оператор у мові програмування C++🎯 Мета роботиОзнайомитися з принципом роботи умовних операторів $if$, $if...else$, $if...else\ if...else$, а також тернарного оператора $?:$. Навчитися використовувати їх для розгалуження алгоритмів у програмах.
 Для чого використовується умовний оператор у мові програмування C++?Для розгалуження алгоритму. Він дозволяє виконати певні дії лише за умови виконання заданої логічної умови.2. Який загальний синтаксис оператора if?$$if\ (умова)\ {\ оператор;\ }\ $$3. Чим відрізняється конструкція if від if...else?$if$ виконує дію лише коли умова істинна. $if...else$ надає альтернативну дію ($else$) на випадок, коли умова хибна.4. Як працює структура if...else if...else?Вона послідовно перевіряє низку умов. Виконується блок коду першої істинної умови. Якщо жодна умова не істинна, виконується блок $else$ (якщо він є).5. Як можна замінити простий умовний оператор if...else тернарним оператором, наведіть приклад.Тернарний оператор $?:$ використовується для скороченого присвоєння значення на основі умови.Приклад: статус = (вік < 18) ? "Неповнолітній" : "Повнолітній";6. Які типи виразів можуть бути використані як умова в операторі if?Будь-який вираз, який можна інтерпретувати як логічне значення ($bool$). Це можуть бути порівняння ($x > 5$), логічні операції $(\&\&, \|)$, або цілі числа (де 0 — $false$, а будь-яке інше число — $true$).7. Що таке вкладені умовні оператори і коли доцільно їх використовувати?Це оператори $if/else$, розташовані всередині іншого блоку $if$ або $else$. Вони потрібні для перевірки додаткових, залежних умов, які мають сенс лише після виконання зовнішньої умови.8. Яким чином можна забезпечити коректне виконання кількох операторів усередині блоку if?Необхідно обгорнути ці оператори у фігурні дужки $(\{\...\})$ для створення складеного оператора (блоку коду).9. Які можливі помилки можуть виникати під час використання умовного оператора?Використання оператора присвоєння $(=)$ замість оператора порівняння $(==)$.Пропуск фігурних дужок $(\{\...\})$ при використанні кількох операторів.Неправильний порядок перевірки умов в конструкції if...else if...else.10. Яку роль відіграють логічні оператори $(\&\&, \|, !)$ в умовних виразах?Вони дозволяють комбінувати або заперечувати прості умови:$\&\&$ (AND): Умова істинна, якщо обидва операнди істинні.$\|$ (OR): Умова істинна, якщо хоча б один операнд істинний.$!$ (NOT): Заперечує логічне значення.
